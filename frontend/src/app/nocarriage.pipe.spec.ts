import { NocarriagePipe } from './nocarriage.pipe';

describe('NocarriagePipe', () => {
  it('create an instance', () => {
    const pipe = new NocarriagePipe();
    expect(pipe).toBeTruthy();
  });
});
